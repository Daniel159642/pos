# No external dependencies required for basic SQLite usage
# Python 3.6+ includes sqlite3 in the standard library

# Optional dependencies for document scraping:
pdfplumber>=0.9.0  # For PDF scraping
pandas>=1.3.0      # For Excel/CSV scraping
openpyxl>=3.0.0    # For Excel file support (required by pandas for .xlsx files)

# Web viewer dependencies:
Flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
eventlet>=0.33.0

# Image matching dependencies (Deep Learning):
torch>=2.0.0        # PyTorch for deep learning
torchvision>=0.15.0 # Pre-trained models and image transforms
Pillow>=9.0.0       # Image processing
numpy>=1.21.0       # Numerical operations
scikit-learn>=1.0.0 # For cosine similarity (optional, we implement our own)

# Barcode scanning dependencies:
pyzbar>=0.1.9       # Barcode and QR code scanning
opencv-python>=4.5.0 # Image processing for barcode detection

# Calendar integration dependencies:
icalendar>=5.0.0    # iCal/Google Calendar support
pytz>=2023.3        # Timezone support

# Metadata extraction dependencies (FREE - no paid APIs):
spacy>=3.4.0        # Natural language processing (runs locally)
fuzzywuzzy>=0.18.0  # Fuzzy string matching
python-Levenshtein>=0.12.0  # Required by fuzzywuzzy for better performance
requests>=2.28.0    # For free barcode lookup APIs (Open Food Facts)
ollama>=0.1.0       # Optional: Local LLM for better category naming (hybrid approach)

# Receipt generation dependencies:
reportlab>=4.0.0   # PDF generation for receipts
qrcode>=7.4.0       # QR code/barcode generation for receipts
Pillow>=9.0.0       # Image processing (already listed above, but needed for qrcode)
