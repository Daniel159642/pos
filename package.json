{
  "name": "pos",
  "version": "1.0.0",
  "private": true,
  "description": "POS System with desktop (Tauri) and web frontend",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "shopify:link": "shopify app config link",
    "shopify:deploy": "shopify app deploy"
  },
  "devDependencies": {
    "@shopify/cli": "^3.88.0",
    "@tauri-apps/cli": "^2",
    "jsonwebtoken": "^9.0.3"
  }
}
