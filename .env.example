# Environment Variables for POS System
# Copy this file to .env and fill in your actual values

# -----------------------------------------------------------------------------
# Database (backend only - never expose to client/Electron app)
# Use DATABASE_URL for a single connection string (recommended for Supabase).
# -----------------------------------------------------------------------------
# Supabase: Project Settings → Database → Connection string (URI, Session pooler)
# Local:    postgresql://postgres:postgres@localhost:5432/pos_db
DATABASE_URL=

# Optional: connection pool size. Defaults: min 1, max 3 (Supabase Session mode has a low limit).
# For local PostgreSQL you can raise: DB_POOL_MAX=10
# DB_POOL_MIN=1
# DB_POOL_MAX=3

# Optional: use these instead of DATABASE_URL for local PostgreSQL
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=pos_db
# DB_USER=postgres
# DB_PASSWORD=postgres

# -----------------------------------------------------------------------------
# Clerk Authentication (Backend)
# -----------------------------------------------------------------------------
CLERK_SECRET_KEY=

# Stripe Payment Processing
STRIPE_SECRET_KEY=

# Encryption Key (Optional)
ENCRYPTION_KEY=

# AI-powered shipment document processor (OpenAI)
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Shopify OAuth (Connect with Shopify)
# From Swiftly app: Partners → Swiftly → Configuration → API credentials
# -----------------------------------------------------------------------------
# Client ID from shopify.app.toml (Swiftly: adbc3619701b075ebb8f9882030ee370)
SHOPIFY_CLIENT_ID=
# Client secret from Partners (Apps → Swiftly → Configuration → API credentials)
SHOPIFY_CLIENT_SECRET=
# Optional: override callback URL (default: request.host_url + /api/integrations/shopify/callback)
# SHOPIFY_REDIRECT_URI=https://your-pos-domain.com/api/integrations/shopify/callback
